import 'package:flutter/material.dart';

class CustomerMessageTab extends StatelessWidget {
  const CustomerMessageTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Message (đang phát triển)'));
  }
}
