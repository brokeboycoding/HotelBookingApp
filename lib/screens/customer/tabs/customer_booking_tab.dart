import 'package:flutter/material.dart';

class CustomerBookingTab extends StatelessWidget {
  const CustomerBookingTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('My Booking (đang phát triển)'));
  }
}
